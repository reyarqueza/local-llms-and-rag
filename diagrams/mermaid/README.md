# Mermaid Sources

Store Mermaid source files in this directory using the `.mmd` extension. The build script renders them into vector assets under `assets/diagrams/`.

For PDF chapters, reference the generated `.pdf` output to preserve vector rendering in the final document.
